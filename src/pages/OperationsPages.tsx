import { useCallback, useState } from 'react'
import { AlertOctagon, Bell, Check, Download, FileText, ShieldAlert } from 'lucide-react'
import { useService } from '../hooks/useService'
import { services } from '../services/mockServices'
import { DataTable, DirectionBadge, EnvironmentBadge, FilterBar, LoadingState, MetricCard, PageHeader, Panel, PnLDisplay, RiskGauge, StatusBadge } from '../components/ui'
import { EquityChart, PerformanceChart } from '../components/TradingCharts'

const Button = ({children,tone='',disabled=false,onClick}:{children:React.ReactNode;tone?:string;disabled?:boolean;onClick?:()=>void})=><button className={`btn ${tone}`} disabled={disabled} onClick={onClick}>{children}</button>

export function PendingOrders() {
 const rows=[['SIM-20412','EURUSD','Buy Limit','1.00','1.10840','1.10320','1.11960','GTC','EMA Pullback','08:12 UTC'],['SIM-20411','XAUUSD','Sell Stop','0.40','2629.00','2641.00','2602.00','Today','Gold Scalper','07:58 UTC'],['SIM-20408','NAS100','Buy Stop','0.25','19820.00','19720.00','20040.00','GTC','Range Breakout','06:44 UTC']]
 return <><PageHeader title="Pending Orders" description="Virtual instructions awaiting mock market conditions." actions={<EnvironmentBadge/>}/><Panel title="Simulation pending orders" subtitle="No order is transmitted externally"><FilterBar/><DataTable columns={['Ticket','Symbol','Order Type','Volume','Entry','SL','TP','Expiry','Strategy','Created','Status','Actions']} rows={rows.map(r=>[...r,<StatusBadge tone="warning">PENDING</StatusBadge>,<div className="row-actions"><button>View</button><button>Modify</button><button>Cancel</button></div>])}/></Panel></>
}
export function TradeHistory() {
 const load=useCallback(()=>services.trades.getTrades(),[]); const {data,loading}=useService(load); const [selected,setSelected]=useState(false); if(loading||!data)return <LoadingState/>
 return <><PageHeader title="Trade History" description="Closed simulation deals with complete cost and outcome accounting."/><Panel title="Closed trades" subtitle="Simulation ledger"><FilterBar>{['Date range','Account','Symbol','Strategy','Direction','Result'].map(x=><Button tone="ghost">{x}</Button>)}</FilterBar><DataTable columns={['Ticket','Symbol','Direction','Strategy','Volume','Entry','Exit','SL','TP','Gross P/L','Commission','Swap','Net P/L','Duration','Closed At']} rows={data.map(t=>[<button className="link" onClick={()=>setSelected(true)}>{t.ticket}</button>,t.symbol,<DirectionBadge value={t.direction}/>,t.strategy,t.volume,t.entry,t.exit,t.stopLoss,t.takeProfit,<PnLDisplay value={t.grossPnl}/>,t.commission,t.swap,<PnLDisplay value={t.netPnl}/>,t.duration,t.closedAt])}/></Panel>{selected&&<div className="drawer"><button className="drawer-close" onClick={()=>setSelected(false)}>×</button><span className="eyebrow">TRADE DETAIL</span><h2>SIM-10321</h2><p>This deal was generated by the simulation ledger. No broker fill exists.</p><div className="detail-list">{[['Order','SIM-10320'],['Deal','DEMO-D-8751'],['Position','SIM-P-319'],['Environment','SIMULATION'],['Result','+$490.40']].map(([l,v])=><div><span>{l}</span><strong>{v}</strong></div>)}</div></div>}</>
}
export function RiskManagement() {
 const load=useCallback(()=>Promise.all([services.risk.getProfile(),services.risk.getEvents()]),[]); const {data,loading}=useService(load); const [stopped,setStopped]=useState(false); if(loading||!data)return <LoadingState/>; const [p,events]=data
 return <><PageHeader title="Risk Management" description="Portfolio safeguards and simulation-only emergency controls." actions={<Button tone="danger" onClick={()=>setStopped(!stopped)}><AlertOctagon/>{stopped?'RESUME SIMULATION':'EMERGENCY STOP'}</Button>}/>{stopped&&<div className="danger-banner"><ShieldAlert/> Simulation halted. No mock orders or signals will be accepted until resumed.</div>}<div className="gauge-grid"><RiskGauge label="Daily loss usage" value={1.2} limit={p.maxDailyLoss}/><RiskGauge label="Weekly loss usage" value={2.8} limit={p.maxWeeklyLoss}/><RiskGauge label="Current drawdown" value={2.4} limit={p.maxDrawdown}/><RiskGauge label="Open account risk" value={3.2} limit={p.maxOpenRisk}/><RiskGauge label="Margin level" value={1846} limit={p.minMarginLevel} inverse/></div><div className="two-grid"><Panel title="Risk profile" subtitle="Balanced Simulation"><div className="settings-list">{Object.entries(p).map(([k,v])=><div><span>{k.replace(/([A-Z])/g,' $1')}</span><strong>{v}</strong></div>)}</div></Panel><Panel title="Risk events" subtitle="Latest simulation controls"><div className="event-list">{events.map(e=><div><StatusBadge tone={e.severity==='WARNING'?'warning':'info'}>{e.severity}</StatusBadge><p><strong>{e.title}</strong><span>{e.detail}</span><small>{e.occurredAt}</small></p></div>)}</div></Panel></div><Panel title="Portfolio & exposure" subtitle="Concentration and correlation overview"><div className="exposure-grid">{[['USD currency exposure',72],['XAUUSD symbol risk',38],['Long exposure',61],['Short exposure',39],['Trend strategies',44],['Forex asset class',57]].map(([l,v])=><div><span>{l}</span><div><i style={{width:`${v}%`}}/></div><strong>{v}%</strong></div>)}</div><div className="placeholder-note">Correlation matrix and cross-account exposure become authoritative after backend portfolio aggregation in a future phase.</div></Panel></>
}
export function PaperTrading() {
 return <><PageHeader title="Paper Trading" description="Virtual capital workspace using simulated data in Phase 1." actions={<StatusBadge tone="info">SIMULATED DATA</StatusBadge>}/><div className="metrics-grid"><MetricCard label="Virtual Balance" value="$50,000.00"/><MetricCard label="Virtual Equity" value="$51,284.60"/><MetricCard label="Open Paper Positions" value="3"/><MetricCard label="Paper P/L" value={<PnLDisplay value={1284.6}/>}/></div><div className="info-banner"><FileText/> Paper trading will eventually use real prices with virtual capital. Phase 1 uses deterministic simulated market data.</div><Panel title="Paper positions" subtitle="Virtual ledger"><DataTable columns={['Ticket','Symbol','Direction','Volume','Entry','Current','P/L','Opened']} rows={[['PAPER-082','XAUUSD',<DirectionBadge value="BUY"/>,'0.20','2638.40','2642.20',<PnLDisplay value={76}/>,'2h 18m'],['PAPER-079','EURUSD',<DirectionBadge value="SELL"/>,'0.50','1.11720','1.11482',<PnLDisplay value={119}/>,'5h 42m']]}/></Panel></>
}
export function Analytics() {
 const load=useCallback(()=>services.analytics.getEquitySeries(),[]);const{data,loading}=useService(load);if(loading||!data)return<LoadingState/>
 return <><PageHeader title="Performance Analytics" description="Professional simulation breakdown across trading dimensions."/><div className="metrics-grid"><MetricCard label="Average R Multiple" value="1.84R"/><MetricCard label="Profit Factor" value="1.72"/><MetricCard label="Max Drawdown" value="7.4%"/><MetricCard label="Long vs Short" value="58 / 42"/><MetricCard label="Winning vs Losing" value="68 / 32"/></div><div className="two-grid"><Panel title="Equity and drawdown" subtitle="12-month simulation"><EquityChart data={data}/></Panel><Panel title="Weekday performance" subtitle="Net P/L by day"><PerformanceChart/></Panel></div>{['Strategy','Symbol','Timeframe','Weekday','Trading Session'].map((d,i)=><Panel title={`Performance by ${d}`} subtitle="Simulation aggregation"><div className="ranking-list">{['EMA Pullback','Market Structure','London Breakout','Gold Scalper'].map((x,n)=><div><span>{n+1}</span><p><strong>{i?`${d} segment ${n+1}`:x}</strong><small>{18+n*7} trades · {(64-n*2.4).toFixed(1)}% win rate</small></p><b className="positive">+${(4280-n*620).toLocaleString()}</b></div>)}</div></Panel>)}</>
}
export function Reports() {
 const [category,setCategory]=useState('Daily'); return <><PageHeader title="Reports" description="Review simulation summaries before future server-side export support." actions={<Button disabled><Download/> Export unavailable</Button>}/><div className="report-categories">{['Daily','Weekly','Monthly','Account','Strategy','Risk','Execution'].map(x=><button className={category===x?'active':''} onClick={()=>setCategory(x)}><FileText/><span>{x}</span></button>)}</div><Panel title={`${category} Report Preview`} subtitle="September 17, 2026 · SIMULATION"><div className="report-preview"><div><span>NEXA TRADEBOT</span><EnvironmentBadge/></div><h2>{category} Trading Performance</h2><p>Prepared from deterministic Phase 1 mock records.</p><div className="report-kpis"><div><span>Net P/L</span><strong className="positive">+$1,842.30</strong></div><div><span>Trades</span><strong>12</strong></div><div><span>Win rate</span><strong>68.4%</strong></div><div><span>Risk events</span><strong>1 warning</strong></div></div><p className="placeholder-note">PDF, Excel, and CSV actions remain disabled until valid server-generated exports are implemented. No fake download is provided.</p></div></Panel></>
}
export function NewsCalendar() {
 const events=[['08:30','USD','High','Core Retail Sales m/m','0.4%','0.3%','0.5%'],['09:00','EUR','Medium','ECB Economic Bulletin','—','—','—'],['12:30','USD','High','Initial Jobless Claims','263K','260K','258K'],['14:00','GBP','Low','MPC Member Speech','—','—','—'],['23:30','JPY','Medium','National CPI y/y','2.8%','2.9%','—']]
 return <><PageHeader title="News Calendar" description="Demonstration economic events; not a live news feed."/><Panel title="Thursday, September 17" subtitle="All times UTC · MOCK CALENDAR"><FilterBar>{['Currency','Impact','Date'].map(x=><Button tone="ghost">{x}</Button>)}</FilterBar><DataTable columns={['Time','Currency','Impact','Event','Previous','Forecast','Actual']} rows={events.map(e=>[e[0],<b>{e[1]}</b>,<StatusBadge tone={e[2]==='High'?'bad':e[2]==='Medium'?'warning':'neutral'}>{e[2]}</StatusBadge>,e[3],e[4],e[5],e[6]])}/></Panel><div className="placeholder-note"><ShieldAlert/> News Trading Protection is planned for a future phase. It does not block simulation activity in Phase 1.</div></>
}
export function MT5Accounts() {
 const load=useCallback(()=>services.account.getAccounts(),[]);const{data,loading}=useService(load);if(loading||!data)return<LoadingState/>
 return <><PageHeader title="MT5 Accounts" description="Connection inventory only. Nexa TradeBot never requests broker passwords in Phase 1." actions={<StatusBadge tone="bad">NOT CONNECTED</StatusBadge>}/><div className="account-card">{data.map(a=><><div className="account-icon">XM</div><div><span className="eyebrow">{a.environment} ACCOUNT</span><h2>{a.name}</h2><p>{a.broker} · {a.platform}</p></div><div className="account-details">{[['Server',a.server],['Currency',a.currency],['Leverage',a.leverage],['Status',a.status]].map(([l,v])=><p><span>{l}</span><strong>{v}</strong></p>)}</div><Button disabled>Connection unavailable in Phase 1</Button></>)}</div><div className="warning-box"><ShieldAlert/><p><strong>Credential-safe foundation</strong><span>No password, investor password, API secret, or terminal path is collected or stored. MT5 connectivity belongs to a later DEMO-only phase.</span></p></div></>
}
export function Notifications() {
 const load=useCallback(()=>services.notifications.getNotifications(),[]);const{data,loading}=useService(load);const[read,setRead]=useState(false);if(loading||!data)return<LoadingState/>
 return <><PageHeader title="Notifications" description="Signal, risk, strategy, system, and security activity." actions={<Button tone="ghost" onClick={()=>setRead(true)}><Check/>Mark all read</Button>}/><FilterBar>{['All','Trade','Signal','Risk','Strategy','MT5','News','System','Security'].map(x=><button className="chip">{x}</button>)}</FilterBar><Panel title="Notification center" subtitle={`${read?0:data.filter(n=>!n.read).length} unread`}><div className="notification-list">{data.map(n=><div className={!n.read&&!read?'unread':''}><span className="notification-icon"><Bell/></span><StatusBadge tone={n.category==='Risk'?'warning':n.category==='Signal'?'purple':'info'}>{n.category}</StatusBadge><p><strong>{n.title}</strong><span>{n.body}</span></p><time>{n.time}</time></div>)}</div></Panel></>
}
export function SystemHealthPage() {
 const load=useCallback(()=>services.health.getHealth(),[]);const{data,loading}=useService(load);if(loading||!data)return<LoadingState/>
 return <><PageHeader title="System Health" description="Operational readiness across simulation and future integration boundaries." actions={<StatusBadge tone="good">PHASE 1 HEALTHY</StatusBadge>}/><div className="health-meta">{[['App version',data.version],['Last heartbeat',data.lastHeartbeat],['Market update',data.lastMarketUpdate],['Last signal',data.lastSignal]].map(([l,v])=><div><span>{l}</span><strong>{v}</strong></div>)}</div><div className="health-grid">{data.services.map(s=><article><div className={`health-icon ${s.state.toLowerCase().replace(' ','-')}`}><span/></div><p><strong>{s.name}</strong><span>{s.detail}</span></p><StatusBadge tone={s.state==='ONLINE'?'good':s.state==='NOT CONNECTED'?'bad':s.state==='MOCK'?'info':'warning'}>{s.state}</StatusBadge></article>)}</div></>
}
export function AuditLogs() {
 const rows=[['08:48:12','Simulation Admin','UPDATE','Risk','Adjusted max open risk from 5% to 6%','127.0.0.1','SUCCESS'],['08:42:08','System','WARNING','Risk','USD exposure crossed warning threshold','system','SUCCESS'],['08:31:44','Simulation Admin','SIMULATE','Manual Trading','Created virtual XAUUSD order SIM-10482','127.0.0.1','SUCCESS'],['08:21:05','Mock Signal Engine','CREATE','Signals','Generated simulated XAUUSD signal','system','SUCCESS'],['07:58:19','System','BLOCK','Auto Trading','Rejected unavailable execution request','system','BLOCKED']]
 return <><PageHeader title="Audit Logs" description="Tamper-evident interface preview using simulated security events."/><Panel title="Activity ledger" subtitle="Phase 1 in-memory events"><FilterBar>{['User','Module','Action','Result','Date'].map(x=><Button tone="ghost">{x}</Button>)}</FilterBar><DataTable columns={['Timestamp','User','Action','Module','Description','IP','Result']} rows={rows.map(r=>[...r.slice(0,6),<StatusBadge tone={r[6]==='SUCCESS'?'good':'bad'}>{r[6]}</StatusBadge>])}/></Panel></>
}
export function Settings() {
 const[t,setT]=useState('General'); return <><PageHeader title="Settings" description="Local simulation preferences and future configuration boundaries."/><div className="settings-layout"><nav>{['General','Trading','Risk','Notifications','Security','Appearance','System'].map(x=><button className={t===x?'active':''} onClick={()=>setT(x)}>{x}</button>)}</nav><Panel title={`${t} Settings`} subtitle="Phase 1 local preference preview"><div className="settings-form">{t==='Appearance'?<><div className="setting-row"><p><strong>Color theme</strong><span>Dark professional terminal is the Phase 1 default.</span></p><select><option>Dark (default)</option><option>System (future)</option></select></div><div className="setting-row"><p><strong>Compact density</strong><span>Optimize tables for trading workstations.</span></p><input type="checkbox" defaultChecked/></div></>:['Simulation confirmations','Sound alerts','Desktop notifications','Compact tables','Risk warning banners'].map((x,i)=><div className="setting-row"><p><strong>{x}</strong><span>{i?'Stored locally for the demonstration.':'Require confirmation before virtual state changes.'}</span></p><input type="checkbox" defaultChecked={i!==1}/></div>)}</div><Button tone="primary">Save local preferences</Button></Panel></div></>
}
