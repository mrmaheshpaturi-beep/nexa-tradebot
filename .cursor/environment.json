{
  "name": "Nexa TradeBot",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend-api",
      "command": "cd backend && php artisan serve --host=0.0.0.0 --port=43128",
      "description": "Laravel 13 API (session auth, SQLite). Serves /api/v1 and /sanctum."
    },
    {
      "name": "frontend",
      "command": "npm run dev -- --host=0.0.0.0 --port=43127",
      "description": "React 19 + Vite dev server. Proxies /api and /sanctum to the backend on port 43128."
    }
  ],
  "ports": [
    { "name": "frontend", "port": 43127 },
    { "name": "backend-api", "port": 43128 }
  ]
}
