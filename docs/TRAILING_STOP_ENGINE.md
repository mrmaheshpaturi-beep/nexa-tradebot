# Trailing Stop Engine

Types: FIXED_DISTANCE, ATR_BASED, PERCENTAGE, STRUCTURE_BASED. Honors start + step. Never loosens risk. Broker stops-level + digit precision validated. Idempotent (no-op if below step / no improvement).
